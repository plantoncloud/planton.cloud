'use client';

import { Box, styled } from '@mui/material';

export const StyledDashboardImg = styled(Box)(() => ({
  backgroundImage:
    'linear-gradient(to bottom, rgba(0,0,0,0) 60%, rgba(0,0,0,0.95) 100%), url(/images/home/dashboard.svg)',
}));
